#include <stdio.h>
#include <string.h>
#include "voter.h"

int main(void) {
    printf("Are you existing user? ");
    char u;
    scanf("%c", u);
    if (u == 'Y') {
        // login
    } else {
        // register as user
    }

    return 0;
}
